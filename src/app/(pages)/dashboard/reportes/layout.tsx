import { type Metadata } from "next";
import { type PropsWithChildren } from "react";

export const metadata: Metadata = {
  title: "Animal Hostel | Reportes",
};

export default function ReportesLayout({ children }: PropsWithChildren) {
  return children;
}
