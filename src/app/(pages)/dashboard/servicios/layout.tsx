import type { Metadata } from "next";
import type { PropsWithChildren } from "react";

export const metadata: Metadata = {
  title: "Animal Hostel | Servicios",
};

export default function ServiciosLayout({ children }: PropsWithChildren) {
  return children;
}
